# G School EMS (v2) — Full Replacement

Self-hosted emergency notification for schools (SRP: HOLD, SECURE, SHELTER, EVACUATE, LOCKDOWN).
Includes:
- Trigger UI (DRILL / REAL)
- Admin UI (Clockwise URLs, PBX paging extension, Cisco phones & TFTP audio)
- HTML Emails (icons) to responder_alerts@gdistrict.org
- FreePBX/Asterisk paging via AMI
- Cisco phones XML push: Display text + play TFTP tone.raw
- Clockwise Campus webhooks

See `config.yaml` for settings.
